@model IEnumerable<DocumentFlow.Models.ViewModels.RecentLoginViewModel>
@{
    ViewData["Title"] = "Мониторинг сессий";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">
        <i class="bi bi-activity me-2"></i>Мониторинг сессий
    </h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-action="Index">IT Администрирование</a></li>
        <li class="breadcrumb-item active">Мониторинг сессий</li>
    </ol>

    <div class="card mb-4">
        <div class="card-header">
            <i class="bi bi-clock-history me-1"></i>
            История входов в систему
        </div>
        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover table-striped">
                        <thead class="table-dark">
                            <tr>
                                <th>Пользователь</th>
                                <th>Email</th>
                                <th>Роли при входе</th>
                                <th>Время входа</th>
                                <th>IP-адрес</th>
                                <th class="text-center">Действия</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var login in Model)
                            {
                                <tr>
                                    <td>
                                        <i class="bi bi-person-circle me-2"></i>
                                        <strong>@login.UserName</strong>
                                    </td>
                                    <td>@login.Email</td>
                                    <td>
                                        @foreach (var role in login.Roles.Take(3))
                                        {
                                            <span class="badge bg-primary me-1">@role</span>
                                        }
                                        @if (login.Roles.Count > 3)
                                        {
                                            <span class="badge bg-secondary">+@(login.Roles.Count - 3) ещё</span>
                                        }
                                        @if (!login.Roles.Any())
                                        {
                                            <span class="text-muted">Нет ролей</span>
                                        }
                                    </td>
                                    <td>
                                        <span class="text-nowrap">
                                            @login.LoginTime.ToLocalTime().ToString("dd.MM.yyyy")
                                        </span>
                                        <br />
                                        <small class="text-muted">
                                            @login.LoginTime.ToLocalTime().ToString("HH:mm:ss")
                                        </small>
                                    </td>
                                    <td>
                                        <code>@(login.IpAddress ?? "Неизвестен")</code>
                                    </td>
                                    <td class="text-center">
                                        <a asp-action="UserLoginHistory" asp-route-id="@login.UserId" 
                                           class="btn btn-sm btn-outline-primary" title="История пользователя">
                                            <i class="bi bi-clock-history"></i>
                                        </a>
                                        <a asp-action="EditRoles" asp-route-id="@login.UserId" 
                                           class="btn btn-sm btn-outline-secondary" title="Редактировать права">
                                            <i class="bi bi-shield-lock"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <i class="bi bi-inbox" style="font-size: 4rem; color: #6c757d;"></i>
                    <h5 class="mt-3 text-muted">Нет записей о входах</h5>
                    <p class="text-muted">История входов пользователей появится здесь после их авторизации.</p>
                </div>
            }
        </div>
    </div>

    <!-- Легенда -->
    <div class="card">
        <div class="card-header">
            <i class="bi bi-info-circle me-1"></i>
            Информация
        </div>
        <div class="card-body">
            <p class="mb-0">
                <i class="bi bi-lightbulb me-2"></i>
                Здесь отображаются последние 100 входов пользователей в систему. 
                Каждая запись показывает, под какими ролями пользователь авторизовался.
                Для просмотра полной истории конкретного пользователя нажмите на кнопку 
                <span class="badge bg-outline-primary"><i class="bi bi-clock-history"></i></span>.
            </p>
        </div>
    </div>
</div>
