@{
    ViewData["Title"] = "Task 3 - XSLT Transformation";
}

<div class="container mt-4">
    <h2>@ViewData["Title"]</h2>
    <p>Transforms `Data.xml` into HTML using `Style.xslt` through `System.Xml.Xsl.XslCompiledTransform`.</p>

    <div class="mt-4 border p-3 rounded bg-light">
        @Html.Raw(ViewBag.HtmlResult)
    </div>
</div>